---
title: Version 2.4
sidebar_position: 1
---

# C-Play Version 2.4

This is the landing page for the documentation of C-Play Version 2.4, from the application itself. This version is currently in beta on a separate development branch; when it becomes the latest release, the continuously updated guides below apply to this release.

This update introduces NDI output — C-Play can now publish its main video and individual presentation layers as NDI sources that other applications (e.g. OBS Studio) can receive over the network. It also adds two new live input layer types — **WebRTC** (WHEP streams, e.g. from MediaMTX) and **DirectShow** (Windows camera/capture card input or media files rendered through DirectShow) — plus multi-video composition for nodes, master-only layers, and several 3D view improvements.

#### NDI output
C-Play v2.4 can send its own video out through NDI: in addition to the full player output, each presentation layer can be published as an independent NDI source from the master. The main output follows the [master view](/playback/views#master-view) — while a 3D view mode is active it publishes the rendered 3D scene instead of the main video, at a selectable resolution (2K–8K). Video only for now. See [C-Play -> NDI output](/system-integration/ndi-output).

#### New live input layers: WebRTC and DirectShow
Two new layer types can be added in the *Add new layer* dialog of the presentation tool:

* **WebRTC** — pulls a WHEP stream over the network by URL, for example from [MediaMTX](https://github.com/bluenviron/mediamtx). Video (H.264/H.265) and audio (Opus) are decoded with FFmpeg, using NVDEC hardware decoding when available, and a failed session is re-established automatically while the layer should be running. Requires a build with WebRTC support (`BUILD_CPLAY_WITH_WEBRTC`, off by default).
* **DirectShow** — Windows only. Capture live from a camera or capture card chosen in the dialog (video and audio devices are listed separately), or render an image/video file through a DirectShow filter graph using the system's built-in decoders. Video for now; enabled by default in Windows builds (`BUILD_CPLAY_WITH_DIRECTSHOW`).

See [Build presentation](/media/cplaypres) for how to add and configure both layer types.

#### Multi-video composition (*.cplaymulti)
Multiple videos can play simultaneously on cluster nodes, all time-synced to a single master reference video, with per-node file paths and different rendering mappings (e.g. left/right eye for stereoscopic domes). See [Multi-video composition](/media/cplaymulti).

#### Master-only layers
Layers in the layer view can now be marked to exist on the master only — not synced to the nodes — with a new **Sync** button. This is useful for example for layers whose purpose is NDI output, or control-only layers. A new user interface setting hides such layers from the 3D view by default. See [Window & UI](/settings/window_and_ui) and [C-Play -> NDI output](/system-integration/ndi-output#master-only-layers-sync-button).

#### 3D view improvements
Zoom in the dome render view, moving selected layers with Ctrl + left mouse click, and saving the camera position/rotation as startup values (double-clicking restores the saved pose). The header taskbar view-mode button is now a menu that selects how the master view renders: only the main video, all layers in a 3D view with a perspective camera, or all layers in a 3D view with a fisheye camera — a new optional **180° fisheye (fulldome) projection** that shows your dome/sphere content as a flat circular image. See [Views](/playback/views#fisheye-180-projection-fulldome) and [Window & UI](/settings/window_and_ui).

#### Other changes
* Optional framesync system for tighter synchronization between nodes.
* Per-file mpv options in playfiles, for example to select if a video should decode alpha or not.
* Optional guessing of grid mode when dragging media onto the layers list.
* Updated to OpenGL 4.6 and latest SGCT, with OpenXR support.

## Application Guides

Here are the current application guides for the settings in C-Play v2.4:

 - [Audio settings](/settings/audio) 
 - [Grid/mapping settings](/settings/grid)
 - [Image settings](/settings/image)
 - [Location settings](/settings/location)
 - [Playback settings](/settings/playback)
 - [Playlist settings](/settings/playlist)
 - [Presentation settings](/settings/presentation)
 - [Text & subtitles](/settings/subtitles)
 - [Window & UI](/settings/window_and_ui) *UPDATED: master-only layers in 3D view, NDI output resolution for the 3D view*
 - [Mouse + Keyboard shortcuts](/settings/shortcuts_mouse_keys)